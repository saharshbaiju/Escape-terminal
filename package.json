{
  "name": "escape-terminal-web",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile-friendly web build of Escape the Terminal — same game, no backend.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
